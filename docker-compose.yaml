services:
  web-client:
    build:
      context: .
      dockerfile: ./web-client/Dockerfile
    ports: 
      - "5173:5173"

  web-server:
    build:
      context: .
      dockerfile: ./web-server/Dockerfile
    ports: 
      - "3000:3000"
